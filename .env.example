# Copia este archivo a `.env` y rellena los valores.
# El Client ID de Azure es PÚBLICO (no es un secreto): va embebido en el launcher.

# Id. de aplicación (cliente) de la app de Azure/Entra para el login premium.
AZURE_CLIENT_ID=
